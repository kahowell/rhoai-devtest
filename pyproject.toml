[project]
name = "rhoai-devtest"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Kevin Howell", email = "khowell@redhat.com" }
]
requires-python = ">=3.14"
dependencies = [
    "requests>=2.34.2",
    "rh-aws-saml-login",
]

[project.scripts]
rhoai-devtest = "rhoai_devtest:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
